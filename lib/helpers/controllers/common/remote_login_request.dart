// import 'package:http/http.dart' as http;

import 'dart:developer';

Future<dynamic> processLoginSubmitMaster(
    enteredUserName, enteredUserPassword) async {
  try {} catch (e) {
    log('error processing login submit request');
    log(e.toString());
  }
}

Future<dynamic> processLoginSubmitParent(
    enteredUserName, enteredUserPassword) async {
  try {} catch (e) {
    log('error processing login submit request');
    log(e.toString());
  }
}
